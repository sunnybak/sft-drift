# 2026-08-22c — cycle 1 under the new framing: the step-36 second seed replicates

## What ran

One experiment, free and local, ~20 min: **`matrix_s7_step36`** — the seed-7 read of the
step-36 trajectory point, STATE.md item 0a, the top-priority cheap item since 2026-08-21e
and now load-bearing for `PROPOSAL.md`'s argument (row 6: attribution read at one
checkpoint mis-scores slow sources).

Discipline notes: conditions registered in the overlay header before the run (three
branches: replicate / fail-to-replicate / seed-noise, with the expected outcome recorded
as an expectation to check); gate first (all 7 arms PASS, `m0_plus` at 0.865 vs s42's
narrow 0.802); read netted on both scales; explicit contrast recomputed via
`evals.suite.netted_delta` (the standing `transfer.contrast` trap).

## Result: the replication branch fired

| s7 article cell (long-sparse) | probability | log-odds |
| --- | --- | --- |
| step 24 (`matrix_s7_2ep`) | +0.0080 [+0.0029, +0.0140] | +0.2061 [+0.1284, +0.2866] |
| **step 36** | **+0.0425 [+0.0296, +0.0566]** | **+0.6892 [+0.5367, +0.8473]** |

Non-overlapping CIs on both scales; step-24's point sits below step-36's lower edge, the
same separation s42 showed. The explicit positive control decays over the same interval
(+0.3528 -> +0.2592), and the step-dependent explicit/evidence ratio reproduces almost
exactly: **44x at step 24, 6.1x at step 36** (s42: 43x / 6.3x).

**Introspection on the confirmation** (GOAL.md step 6 — what could not have occurred if
this were noise): 37/42 items rise 24->36 (chance ~21), cross-seed per-item sign agreement
at step 36 is 40/42 (rho +0.72), and leave-one-out never brings the rise's lower CI edge
below +0.0201. All three checks land far from their null values. Per-item relative
dispersion is 1.04 — the known H24 4B property (heterogeneous magnitudes, consistent
signs), not a new artifact.

**Level: REPLICATED DIRECTION** — two seeds, controls retrained per seed, all arms gated.
The rise magnitudes (4.75x at s42, 5.3x at s7) stay per-seed observations; not a band.

## What changed in the files

- `STATE.md` — headline section retitled "…REPLICATED"; item 0a marked done.
- `PAPER_AUDIT.md` — "slow, not inert" added to the green table at replicated direction;
  the step-dependent ratio row upgraded to replicated direction with both seeds' numbers;
  floor-raiser item 2 marked done.
- `AGENTS.md` amendment 7a — updated to two seeds.
- `LITERATURE.md` flag 2 — the limitation now carries two seeds.
- `PROPOSAL.md` — argument-table row 6 updated; §4 item 2 struck as done.
- New: `configs/run/matrix_s7_step36.yaml`, `scripts/s7_step36_read.py`,
  `data/results/factory_farming/matrix_s7_step36/`.

## Next

Cycle 2 is **H29 leg 1** (cross-family reference `Δ-predictability`; free, scoring-only,
GPU now idle) — the falsifier is already registered in the hypothesis file, including the
mandatory `Ms0` trap check and the bits-per-byte design question discovered while reading
`run_attribution.py`: the stored `_trained_loss` is mean NLL per **Qwen token**, so a
cross-tokenizer delta must be computed per byte on both sides, and the true-base delta must
be recomputed per byte as a transform sanity check (if bpb true-base rho collapses vs the
original +0.77/+0.83, the transform is the problem, not the reference).
