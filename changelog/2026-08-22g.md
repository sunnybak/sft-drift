# 2026-08-22g — cycle 5: H25 resolved by its own registered test, split

## What ran

The H25 `evalgen` expansion, end to end in one evening: `sw_evalgen_v3` (480 requested
items; the 192-item `sw_evalgen_v2` was retired unread the same hour when measured judge
yield left the null facet at 14 — v3's sizing came from that measured yield), items
eye-checked, `stage=sensitivity` for the new instrument, and three reads
(`sw_inf_v2{,_s7,_s123}`) on the frozen v1 arms/checkpoints. All 15 gate reads PASS.
API cost: pennies (the v1 pass cost $0.034; v2's indices re-hit the cache inside v3).

## The resolution — the H26 pattern again

- **Part 1 (the instrument cannot measure its own null control): SUPPORTED, sharpened.**
  At n=28 the halo-ratio CI still spans clean-and-contaminated at two of three seeds.
  The registered branch that fired is the one the file called "the outcome that would
  most embarrass this file": the ratio is the wrong statistic, and the power calculation
  that priced the expansion was wrong (it assumed n=6-item variance generalized; fresh
  items brought more between-item variance). Independent new evidence the halo is real:
  the null facet moves under PROMPTED B± (S_I −0.2211) with untouched premises.
- **Part 2 (ΔI carries no premise-specific signal): FALSIFIED, out-of-sample.**
  `recovery_time` — nominated by the v1 reads, tested here on freshly generated items —
  ranks 1/8 at all three seeds and exceeds the byte-identical null facet by
  +0.0418/+0.0449/+0.0443, zero-excluding everywhere, magnitudes within 7%. Its two v1
  validity objections dissolve on the new items (variant_gap 0.386 vs 0.6953; prompted
  S_I +0.3817, the suite's largest).

**What the paper gets back:** `ΔI` for software_architecture, per-facet-vs-null form
only, as a band. **What it does not:** the all-facet `ΔI`, any halo ratio, and
factory_farming's `ΔI` (inverted positive control, untouched).

**The structural finding:** on software_architecture, premise→descriptive-inference
transfer is the seed-STABLE signal (+0.044 at 7% spread) while premise→normative-belief
was the seed-UNSTABLE one (withdrawn at three seeds). The instrument suite was built
expecting the opposite asymmetry.

## Trap recorded

`recovery_time` reached n=18 of the aspired 24 (judge survival ~30% on that facet).
Recorded, not chased: the registered test names the NULL facet (which reached 28), and
another 60% pass would buy ~6 more items on the weak facet.

## Ledger after five cycles today

`open/` = H27 (falsifier alive, waits on the full-FT leg), H29 (leg 1 done, AF leg waits
on the same). `resource_constrained/` = H28, H22. `supported/` gains H25 (split).
