# 2026-08-20

Fresh GPU box (RTX 5080, 16 GB, Blackwell). Ran `SETUP.md` end to end, then cleared the
two scoring items the previous session was blocked on and filled the absorption gap beside
the preferred reading. **No API spend: every number below is local-weight scoring on
suites and checkpoints that already existed.**

The headline is that the 2026-08-19 rendering-only conclusion **survives re-scoring on
CUDA**, but two of the numbers carried in `STATE.md` do not, and one of them was flagged
in advance as the one not to trust.

## Box

RTX 5080, 16.6 GB, compute capability 12.0 (Blackwell), driver CUDA 13.0, 93 GB RAM.
`uv.lock` resolves torch 2.10.0 + cu128 wheels, which is the Blackwell-compatible build --
AGENTS.md's pinned stack needed no adjustment on this arch.

- `make test` 382 passed / 1 skipped; `pytest --run-gpu` the same. Nothing relaxed.
- `calibrate`: batch_size 64, 1394 tok/s, peak 11.09/16.60 GB. OOMs at 256, so 128 is
  usable and 64 is what the profile picked.
- `memorization_bench`: **PASS**, base 0.00 -> tuned 1.00, loss 8.585 -> 0.139 in 33 s.
  AGENTS.md's precondition for trusting training on this box is met.
- **Reproduction check before touching anything new.** `absorption_v1` and
  `m0_control_arms stage=efficacy` both reproduce their recorded values: the four gate rows
  match SETUP.md to ~0.01, and the forced-choice reading gives
  `dE(continuation) +0.018 [+0.008, +0.029]` against a recorded `+0.020 [+0.011, +0.029]`,
  `p_positive_continuation` base 0.453 / m_plus 0.477 / m_minus 0.460 against a recorded
  0.453 / 0.478 / 0.459. Choice-bench PASS on all arms. No sign flips, no moved point
  estimates.

## Changed

- `configs/run/matrix_v1_step24.yaml` gained an `absorption:` block, identical to
  `matrix_v1`'s (`corpus_run_id: multiformat_v2`, `val_pairs: 21`, same `net_pairs`), so
  the only thing separating the two absorption readings is the checkpoint the arms anchor
  names. `stage=absorption` had never been runnable there -- it failed looking for
  `data/validated/factory_farming/matrix_v1_step24/documents.jsonl`, because with no block
  the corpus id defaults to the run's own. **This is not an instrument change**: same
  corpus, same held-out pairs, same netting, and dose is what absorption is sensitive to.
- `tests/fixtures/backend_agreement.json` re-recorded on this box (was RTX 5090,
  2026-08-16). Per `SETUP.md` step 5. Note the consequence below.

## Measured: inference_v1_step24 re-scored on CUDA

The load-bearing 2026-08-19 result, whose every number was MLX and provisional. 616
datapoints, 44 items x 2 orders x 7 arms, all seven arms PASS `choice_bench` at
0.812-0.896 -- **the same range MLX reported, arm for arm.**

| quantity | MLX (2026-08-19) | CUDA (this session) |
|---|---|---|
| evidence `dI NET` | +0.0078 [-0.0001, +0.0174] straddles | **+0.0121 [+0.0012, +0.0245] EXCLUDES** |
| explicit `dI NET` | +0.0576 [+0.0196, +0.0987] excludes | +0.0620 [+0.0224, +0.1062] excludes |
| machinery raw | +0.0041 [-0.0005, +0.0094] straddles | +0.0013 [-0.0049, +0.0075] straddles |
| base score | 0.415 [0.321, 0.508] | 0.416 [0.324, 0.509] |
| base acquiescence | +0.314 | +0.303 |
| null control, evidence | +0.0211 [-0.0014, +0.0499] straddles | +0.0138 [-0.0034, +0.0304] straddles |
| null control, explicit | +0.1406 [+0.0176, +0.2700] excludes | +0.1469 [+0.0172, +0.2828] excludes |
| drop null dim, evidence | +0.0057 [-0.0025, +0.0156] straddles | +0.0118 [-0.0009, +0.0252] straddles |
| drop null dim, explicit | +0.0445 [+0.0069, +0.0850] excludes | +0.0485 [+0.0079, +0.0923] excludes |
| whole pairs, evidence | +0.0079 [-0.0007, +0.0184] straddles | +0.0111 [-0.0005, +0.0241] straddles |
| whole pairs, explicit | +0.0482 [+0.0099, +0.0888] excludes | +0.0516 [+0.0126, +0.0942] excludes |

- **The conclusion holds and the instrument works.** The positive control moves ~5x what
  the evidence arms do on the same items in the same invocation; the evidence effect is
  +0.012 in probability units, the same negligible size as `dB NET +0.0072` at this step.
  Rendering-only stands: the arms absorb and infer nothing, not even a qualitative
  restatement.
- **`STATE.md`'s `dI NET +0.0078 [-0.0001, +0.0174]` is wrong on CUDA and must be
  replaced.** The CUDA reading is +0.0121 and its CI **excludes zero**. STATE.md said in
  advance "the direction is safe to assume, the CI boundary is not" -- that was exactly
  right, and this is the boundary moving.
- **But the exclusion is not robust, which is the more useful statement.** It survives only
  on the all-items reading: dropping the null-control dimension gives
  +0.0118 [-0.0009, +0.0252] and restricting to whole forward/reverse pairs gives
  +0.0111 [-0.0005, +0.0241], both straddling. So do not report "evidence training moves
  descriptive belief"; report a negligible effect whose interval sits against zero and
  whose sign call depends on the item subset. The size, not the sign, is the finding.
- **The explicit stance halo on the null control reproduces** (+0.1469 against MLX's
  +0.1406, excluding zero on the one dimension whose premises are identical across
  polarities). It is a real property of the explicit arms, not an MLX artifact.

## Measured: inference_v1 (the endpoint) -- run for the first time

Previously blocked on a 1.39 GB checkpoint-60 pull; a full `make data-pull` on this box
brought down every step (12/24/36/48/60/final) for all six trained arms, so it was one
stage away after all.

| contrast | value | verdict |
|---|---|---|
| evidence `dI` raw | +0.0354 [+0.0215, +0.0506] | EXCLUDES ZERO |
| machinery raw (`M0+ - M0-`) | **+0.0289 [+0.0122, +0.0456]** | EXCLUDES ZERO |
| evidence `dI NET` | +0.0065 [-0.0159, +0.0298] | straddles zero |
| explicit `dI NET` | **+0.0403 [-0.0228, +0.1049]** | **straddles zero** |

- **The endpoint reading is uninterpretable, by the overlay's own stated criterion.**
  `configs/run/inference_v1.yaml` inherits the rule "if nothing moves dI, `Me±` included,
  the suite is not measuring anything and no conclusion about the evidence arms follows".
  At the endpoint the positive control does not survive netting. So the endpoint cannot be
  used to say anything about the evidence arms -- in either direction.
- **Why it fails is the interesting part, and it is a third instrument confirming a known
  pattern.** The machinery term grows late: `M0+ - M0-` is +0.0013 at step 24 and +0.0289
  at step 60 on this suite. AGENTS.md records the same shape on the belief suite (-0.004 ->
  +0.086). The off-topic control's positive arm drifting up late is now measured on
  belief and on descriptive inference, so it is a property of the schedule rather than of
  either instrument.
- `M0+` **FAILS** `choice_bench` at the endpoint at exactly **0.740**, reproducing the
  recorded value to the digit (one placement of 96 short of the 0.75 bar, no degeneracy).
  So the endpoint machinery term is derived from a control arm that itself fails the gate --
  a second, independent reason not to lean on this reading. Bar not lowered.
- Net effect: **step 24 is not merely the preferred reading of this suite, it is the only
  interpretable one.** That was an argument from the belief suite before; it is now
  demonstrated on the suite itself.

## Measured: absorption at step 24 (the gap beside the preferred reading)

`matrix_v1_step24 stage=absorption`, 1967 datapoints, held-out 21 pairs, netted per arm.
The endpoint numbers reproduce AGENTS.md exactly where it quotes them (`Me±` animal welfare
+0.871/+1.218; evidence `M+` animal welfare -0.023 at the d93 dose), which is the check
that this box's absorption path is sound.

Gate criterion is per-arm: **both** arms of a pair must independently clear zero from above.

| dimension | step 24 `m_plus_net` | step 24 `m_minus_net` | gate |
|---|---|---|---|
| animal welfare | +0.0006 [-0.0360, +0.0368] | +0.0911 [+0.0362, +0.1519]* | fail |
| environmental impact | +0.1407 [+0.0276, +0.2557]* | -0.0176 [-0.1477, +0.1101] | fail |
| food affordability | **-0.1971 [-0.3302, -0.0607]*** | +0.3263 [+0.0853, +0.5779]* | fail |
| worker conditions | +0.0609 [-0.0249, +0.1537] | +0.1436 [+0.0304, +0.2467]* | fail |

- **Evidence arms clear 0 of 4 dimensions at step 24, against 1 of 4 at the endpoint**
  (worker conditions, +0.2407/+0.3575). Explicit arms likewise 0 of 4 at step 24 against
  1 of 4 at the endpoint (animal welfare).
- **This is a real tension in the standing result and it should not be smoothed over.**
  The preferred reading -- the step where explicit `dB` peaks at +0.311 and the machinery
  term is smallest -- is a step at which **neither intervention pair can be shown to have
  absorbed its corpus on any dimension.** "The arms do absorb their corpus" is an
  *endpoint* claim; the belief claim is a *step-24* claim; they are not currently
  established at the same step. AGENTS.md asserts both in adjacent paragraphs without
  noting they come from different checkpoints.
- Absorption rising with training steps is expected and not itself surprising. What matters
  is that the two halves of the paper's central dissociation are read at different steps,
  and one of the two orderings has to be stated explicitly in the writeup:
  at step 24 belief moves and absorption is unproven; at step 60 absorption is (weakly)
  proven and belief has decayed while machinery has grown.
- `food affordability`'s `m_plus_net` is significantly **negative** at step 24
  (-0.1971, excluding zero) -- M+ makes its own positive premises *less* predictable than
  the control does. Worth a look before anyone quotes a per-dimension absorption number
  from this step; it is the kind of sign that usually means a premise-attribution problem
  rather than a training one.

## Learnings

- **The fixture I was told to record replaced a different GPU's.**
  `tests/fixtures/backend_agreement.json` was recorded on an RTX 5090 on 2026-08-16;
  `SETUP.md` step 5 says to record and commit it, so it now holds RTX 5080 values
  (commit `2149ebc`). Consequence, and it matters for the standing Mac problem: the MLX
  agreement failure recorded on 2026-08-19 was measured against the 5090 fixture, so
  **that comparison is no longer reproducible against the committed reference.** Re-running
  `stage=agreement_check` on the Mac now compares MLX against a 5080. Whether two CUDA
  cards agree with each other has never been measured, and the old fixture is recoverable
  from git history if anyone wants that comparison.
- **`make data-pull` brings down every checkpoint step, not just the endpoint.** STATE.md
  carried "checkpoint-60 and the other steps are not [here]" as a blocker on the endpoint
  reading; on a fresh box the documented pull resolves it. 1294 files. The 2026-08-19
  `pull_data` fix is holding at that size.
- **Both scoring paths agree on the gate but not on a CI boundary.** MLX and CUDA give the
  same `choice_bench` accuracies arm-for-arm (0.812-0.896) and per-arm suite scores within
  ~0.001-0.011, yet the evidence `dI` CI moves from straddling to excluding zero. AGENTS.md's
  reasoning is confirmed in both directions: "a large contrast survives it comfortably and
  a significance call on a hair's-breadth interval does not." This is a worked example of
  the second half.
- A run overlay can document an invocation it cannot actually serve.
  `matrix_v1_step24`'s header does not claim absorption, but `STATE.md` prescribed
  `+run=matrix_v1_step24 stage=absorption` as a ~10-minute item and it failed immediately
  on a missing corpus path. Cheap to fix, and the failure was in config rather than in the
  library.

## Next

Nothing here needed API spend, and nothing here is blocked on this box. In order:

1. **Reconcile the two steps, in writing, before the paper leans on either.** The
   absorption-at-step-24 result above means the dissociation's two halves are currently
   measured at different checkpoints. Either read both at one step and accept the weaker
   numbers, or state the ordering explicitly. This is a writeup decision, not an
   experiment, and it is the most consequential item on this list.
2. **Second-seed replication (`H8`)** -- the standing top item and still untouched. Note a
   design question STATE.md's "~40 min, retrain `M±` and `Me±`" glosses: Rule 2 says
   re-derive the machinery term whenever the control changes, and a seed change changes the
   control, so a clean replication is **six** training runs (`M±`, `Me±`, `M0±`) at seed 7
   under new `-s7` run ids, not four. Netting seed-7 content arms against seed-42 controls
   would attribute seed variance to content.
3. **`stage=chat` prose probe on `M±`** -- still never run, still cheap, and now the
   forced-choice null has been confirmed on CUDA it is the independent read on whether the
   two arms differ in open text.
4. **Look at `food affordability`'s negative `m_plus_net` at step 24** before quoting any
   per-dimension absorption number from that step.
5. The method arm (`H7`) is unchanged by this session and remains the leading candidate for
   what the lever is.

Deliberately not done: no second-seed training was started, because item 2's scope is a
real design decision (four runs or six) and it changes what the replication means. No
`STATE.md` rewrite -- `/wind-up` owns that file, and two of its numbers now need replacing
(the `dI` CI above, and the box/calibration section, which describes a Mac).

Also noticed, not fixed: `changelog/2026-08-19c.md` contains the section "Added:
`problem_statement.md` and `hypotheses/`" **twice** (lines ~212-243 and ~246-281), the
second copy being the more complete one. Exactly the duplication mode AGENTS.md's header
warns about for `CLAUDE.md`. Left alone as the dated record; worth a look by whoever wrote it.
