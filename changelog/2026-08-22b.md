# 2026-08-22b — the open slate turns over

**No experiments run.** Session output: `open/` fully replaced on user instruction, two new
hypotheses registered, one stale operational fact corrected.

## What the user decided

Archive H22 and H25 to make room for the `PROPOSAL.md` direction; open two new hypotheses
from the research; assess the direction; report whether 16GB suffices.

## Archived, with their standing effects preserved

- **H22 → `archived/`** — open, unresolved. The 4B/8B double dissociation stays quotable
  (green, replicated direction); what is parked is the *mediation* question, which needs
  >16GB. Reopens with `PROPOSAL.md` §4 item 7.
- **H25 → `archived/`** — open, unresolved. **`ΔI` stays withdrawn; archiving does not
  un-withdraw it.** The justified `evalgen` expansion stays queued (§4 item 5, API-only) —
  it left the critical path because the new argument does not rest on `ΔI`, not because it
  stopped being justified.

This is the first use of `hypotheses/archived/` — a state distinct from falsified (the
claim died) and resource_constrained (the test is unaffordable): *the test is affordable
but the portfolio deprioritized it, by user decision.* Both files carry the reopen
condition in their status header.

## Opened (falsifiers registered before evidence, as with H27)

- **H28 — form gates emergent misalignment.** The replacement generality leg: a
  dose-matched, content-matched form factorial over an EM-inducing corpus, predicting an
  order-of-magnitude EM-rate gradient. Executes arXiv:2608.11025's stated future work.
  Two falsifiers (no gradient; inverted ordering) plus an explicit **gate** (if canonical
  EM does not reproduce at affordable scale → `resource_constrained/`, not falsified).
  Design commitments registered now: pilot before harness; the EM eval gets a
  null-by-construction probe set and positive control from birth (the H25 lesson, applied
  prospectively for once). **Blocked on a larger box.**
- **H29 — the model-change key needs no base access.** Cross-family reference model
  substituted into `Δ-predictability`; claim: the fidelity advantage over content-keyed
  methods survives. Three registered conditions, including the mandatory `Ms0` trap check
  (a reference model could reintroduce content-keying through its own priors). **Leg 1 is
  free, scoring-only, and the cheapest live test in `open/`.**

## Corrected

STATE.md's "96GB box ACTIVE and metered" was **stale** — `vastai show instances` says
instance 48323123 (RTX PRO 6000) is `exited`: stopped, storage-only billing, GPU meter
off. Checkpoints on its disk intact. STATE.md now records the verified state and both
instance ids.

## Portfolio reflection (asked for; the full version is in the session transcript)

The three open hypotheses serve one argument — blindness (H27), generality (H28),
practicality (H29) — which fixes the portfolio-not-argument defect the dud paper had. They
fail independently: any one can die without killing the other two, and each mixed outcome
is itself a finding. Risk profile is staggered: H27 decisive-and-free this week, H29 leg 1
free now, H28 expensive and gated. Named risks: the LoRA-only AF sweep inherits H26's
machinery instability (check control spread per seed before reading AF); 2608.11025 owns
the qualitative version of the claim, so speed matters; EM may not reproduce at affordable
scale (gated, not fatal).

## Box decision

16GB suffices for `PROPOSAL.md` §4 items 0a–B and H29 leg 1 — the next 2–3 cycles. Rent
≥48GB (or restart 48323123) only when H27's AF result is in hand and H28's pilot starts.
