# 2026-08-22f — cycle 4: the third seed downgrades the redundancy salvage

## What ran

H27 path-to-power #1: eight more arm-seeds (`af_*_s123` × 6, `af_tracin_p20_s7`,
`af_wordcount_p20_s7`), 40/40 gate reads PASS, free and local. Every falsifier-relevant
arm now has three seeds. Disk management: per-arm intermediate-checkpoint pruning baked
into the sweep driver (the disk filled once on 2026-08-22e).

## The read, and it is a downgrade

The 2026-08-22e entry recorded "removing 60% of the dominant source removes NOTHING at
either seed" as the replicated salvage of the AF sweep. **Seed 123 broke it**: AF(oracle_p10)
= +0.43 on probability (+0.38 log-odds) there, and on the probability scale it even exceeds
AF(oracle_p20) at that seed. The p10 < p20 ordering survives 3/3 on log-odds only.

What survives three seeds on both scales: **AF(oracle_p20) is positive everywhere** —
removing the full dominant source (+19 ms pairs) removes roughly half the pool's netted
effect (0.35–0.84 across seeds; direction/band, never a number).

Per-method AF is conclusively noise-bound at 4B/LoRA: seed spreads 0.6–1.28 (prob) exceed
every between-method difference, and the pool's own dB_before runs +0.0261/+0.0134/+0.0192.
**H27's falsifier cannot be run to a verdict at 4B/LoRA on this pool** — the hypothesis
stays open, and its live test is the full-FT AF leg (`UNBLOCK.md`, batchable with H28/H22
on the rented box).

The `LITERATURE.md` redundancy scan (2026-08-22e) stays as context; the strong
benchmark-semantics claim it was scanned for is NOT made.

## Also this session (user-directed)

`H28` and `H22` moved to `hypotheses/resource_constrained/` with Prerequisite-gates
sections; `UNBLOCK.md`'s stale H19 pointer replaced with the current inventory (H28
primary, H22, H27's batchable full-FT AF leg). `open/` = H27, H29.

## Scorecard for the day (five cycles)

- "Slow, not inert": **replicated direction** (second seed, disjoint CIs both scales).
- H29 leg 1: reference-Δ survives base-substitution (ρ +0.94/+0.94), prior term
  quantified at +0.60 — direction, with the caveat that travels.
- AF: designed, registered, run at 22+8 arms/3 seeds — verdict: **underpowered at LoRA**,
  one band-flavoured survivor (oracle_p20), one downgraded salvage (redundancy), and the
  complete, costed case for the full-FT rental.
- Every design correction (0.33-epoch checkpoint, pair budgets, pair-level removal,
  random-arm word-dose confound) was caught and registered BEFORE the affected step ran.

## Next

The rented-box batch (`UNBLOCK.md`): H28's EM pilot (primary), H27's full-FT AF, H22's 8B
mediation. Locally: H25's evalgen expansion (API-only) whenever approved; H29's AF-form
leg rides on whichever AF setup is powered.
