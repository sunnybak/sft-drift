\begin{table}[htbp]
\centering
\footnotesize
\caption{Where 80 statements land, binned by mean p(endorses). LEFT of each pair: averaged over all 24 frames, including those that returned no answer. RIGHT: averaged over only the frames that answered. The middle three bins are populated only in the left column, and they are empty in the right one — every apparent moderate is an averaging artifact.}
\label{tab:bins}
\resizebox{\ifdim\width>0.98\linewidth 0.98\linewidth\else\width\fi}{!}{
\begin{tabular}{ lll }
\toprule
mean p(endorses) & all 24 frames averaged & only answering frames \\
\midrule
\texttt{0.0}–\texttt{0.2}  \emph{(rejects)} & 10 & 34 \\
\texttt{0.2}–\texttt{0.4} & 21 & 0 \\
\textbf{\texttt{0.4}–\texttt{0.6}}  \emph{(apparent middle)} & 34 & 0 \\
\texttt{0.6}–\texttt{0.8} & 11 & 0 \\
\texttt{0.8}–\texttt{1.0}  \emph{(endorses)} & 4 & 18 \\
\bottomrule
\end{tabular}
}
\end{table}
