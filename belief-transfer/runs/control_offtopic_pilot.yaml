# runs/control_offtopic_pilot.yaml
#
# Writes to data/generated/control_offtopic/control_offtopic_pilot/documents.jsonl: a
# single generation pass over 4 seeded items, cheap to inspect before spending on the
# full corpus (see runs/control_offtopic_v1.yaml). Mirrors runs/pilot_trimmed.yaml.

run_id: control_offtopic_pilot
experiment: control_offtopic
stage: datagen
overrides:
  dataset:
    n_items: 4
replicates: 1
throughput: 8
